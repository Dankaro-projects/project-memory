name: Verify
on:
  workflow_dispatch:
  workflow_call:
permissions:
  contents: read
jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python: ['3.11', '3.14']
    runs-on: ${{ matrix.os }}
    env:
      PYTHONUTF8: '1'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
      - uses: astral-sh/setup-uv@v6
      - run: python scripts/check_publication.py
      - run: python -u -m unittest discover -s tests -v
        timeout-minutes: 5
      - run: python -m tests.integration.document_case --output results/documents
      - run: python -m tests.integration.host_example --output results/host-example
      - run: node tests/browser/viewer_logic.cjs
      - run: python scripts/build_bundle.py
      - run: uv build
      - run: python scripts/check_artifacts.py dist
      - run: python scripts/installed_smoke.py dist
  browser:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install --no-save playwright@1.55.1
      - run: npx playwright install --with-deps chromium
      - run: python -m tests.integration.host_example --output results/host-example
      - run: node tests/browser/browser_check.cjs
      - run: node tests/browser/workspace_browser.cjs
